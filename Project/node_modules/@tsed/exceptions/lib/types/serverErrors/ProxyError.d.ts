import { ServerException } from "../core/ServerException";
export declare class ProxyError extends ServerException {
    static readonly STATUS = 502;
    name: string;
    constructor(message: string, origin?: Error | string | any);
}
