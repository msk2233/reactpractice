import { ClientException } from "../core/ClientException";
export declare class BadMapping extends ClientException {
    static readonly STATUS = 421;
    name: string;
    constructor(message: string, origin?: Error | string | any);
}
