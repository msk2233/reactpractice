import { RedirectException } from "../core/RedirectException";
export declare class MovedTemporarily extends RedirectException {
    static readonly STATUS = 302;
    name: string;
    constructor(message: string, origin?: Error | string | any);
}
