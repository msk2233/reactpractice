import { classOf } from "./classOf.js";
import { ancestorOf } from "./ancestorOf.js";
import { nameOf } from "./nameOf.js";
export function isInheritedFrom(target, from, deep = 5) {
    if (!target || !from) {
        return false;
    }
    target = classOf(target);
    from = classOf(from);
    while (nameOf(target) !== "") {
        if (!deep) {
            return false;
        }
        if (target === from) {
            return true;
        }
        target = ancestorOf(target);
        deep--;
    }
    return false;
}
//# sourceMappingURL=isInheritedFrom.js.map