import { classOf } from "./classOf.js";
/**
 * Return the prototype of the given class.
 * @param target
 * @returns {any}
 */
export function prototypeOf(target) {
    return classOf(target) === target ? target.prototype : target;
}
//# sourceMappingURL=prototypeOf.js.map