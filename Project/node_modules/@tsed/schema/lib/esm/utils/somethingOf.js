export function makeOf(key, schemes) {
    if (schemes.length) {
        if (schemes.length === 1) {
            return schemes[0];
        }
        else {
            return { [key]: schemes };
        }
    }
    return null;
}
//# sourceMappingURL=somethingOf.js.map