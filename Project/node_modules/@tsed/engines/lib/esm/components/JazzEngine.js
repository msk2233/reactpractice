import { __decorate } from "tslib";
import { ViewEngine } from "../decorators/viewEngine.js";
import { Engine } from "./Engine.js";
let JazzEngine = class JazzEngine extends Engine {
    $compile(template, options) {
        const compile = this.engine.compile(template, options);
        return (options) => new Promise((resolve) => compile.eval(options, resolve));
    }
};
JazzEngine = __decorate([
    ViewEngine("jazz")
], JazzEngine);
export { JazzEngine };
//# sourceMappingURL=JazzEngine.js.map