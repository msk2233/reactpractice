import { __decorate } from "tslib";
import { ViewEngine } from "../decorators/viewEngine.js";
import { Engine } from "./Engine.js";
let HoganEngine = class HoganEngine extends Engine {
    $compile(template, options) {
        const compile = this.engine.compile(template, options);
        return (options) => {
            return compile.render(options, options.partials);
        };
    }
};
HoganEngine = __decorate([
    ViewEngine("hogan", {
        requires: "hogan.js"
    })
], HoganEngine);
export { HoganEngine };
//# sourceMappingURL=HoganEngine.js.map