import { __decorate } from "tslib";
import { ViewEngine } from "../decorators/viewEngine.js";
import { Engine } from "./Engine.js";
let MustacheEngine = class MustacheEngine extends Engine {
    $compile(template, options) {
        return (options) => this.engine.render(template, options, options.partials);
    }
};
MustacheEngine = __decorate([
    ViewEngine("mustache")
], MustacheEngine);
export { MustacheEngine };
//# sourceMappingURL=MustacheEngine.js.map