import { Engine } from "./Engine";
export declare class PugEngine extends Engine {
    protected $compile(template: string, options: any): any;
    protected $compileFile(file: string, options: any): any;
}
