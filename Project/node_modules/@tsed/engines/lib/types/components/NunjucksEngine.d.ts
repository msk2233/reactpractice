import { Engine } from "./Engine";
export declare class NunjucksEngine extends Engine {
    protected configure(options: any): any;
    protected $compile(template: string, options: any): (options: any) => any;
}
