import { Engine } from "./Engine";
export declare class VashEngine extends Engine {
    compile(template: string, options: any): (options: any) => Promise<string>;
    render(template: string, options: any): Promise<string>;
}
