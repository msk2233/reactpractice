import { Engine } from "./Engine";
export declare class DustEngine extends Engine {
    private views;
    private ext;
    configure(options: any): void;
    protected $compile(template: string, options: any): any;
}
