import { LogEvent } from "../../common/core/LogEvent";
import { BaseAppender } from "../../common/appenders/class/BaseAppender";
/**
 * ## Standard Output Appender
 *
 * This appender writes all log events to the standard output stream.
 *
 * ## Configuration
 *
 * * type - stderr
 * * layout - object (optional, defaults to colouredLayout) - see layouts
 *
 * ## Example
 *
 * ```typescript
 * import {Logger} from "@tsed/logger";
 *
 * const logger = new Logger("loggerName");
 *
 * logger.appenders.set("log", {
 *     type: "stdout",
 *     levels: ["info", "trace", "debug"]
 * });
 * ```
 *
 * @private
 */
export declare class StdoutAppender extends BaseAppender {
    private log;
    write(loggingEvent: LogEvent): void;
}
