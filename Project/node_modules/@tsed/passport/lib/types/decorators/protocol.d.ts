import { ProtocolOptions } from "../interfaces/ProtocolOptions";
/**
 * Declare a new Protocol base on a Passport Strategy
 *
 * @decorator
 * @class
 */
export declare function Protocol<T = any>(options: ProtocolOptionsDecorator<T>): any;
export type ProtocolOptionsDecorator<T = any> = {
    name: string;
} & Partial<ProtocolOptions<T>>;
