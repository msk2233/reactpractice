import { BeforeRoutesInit, OnInit, PlatformApplication, Logger } from "@tsed/common";
import { PassportSerializerService } from "./services/PassportSerializerService";
import { ProtocolsService } from "./services/ProtocolsService";
/**
 * @ignore
 */
export declare class PassportModule implements OnInit, BeforeRoutesInit {
    private app;
    private protocolsService;
    private passportSerializer;
    userProperty: string;
    pauseStream: boolean;
    disableSession: boolean;
    platformName: string;
    logger: Logger;
    constructor(app: PlatformApplication, protocolsService: ProtocolsService, passportSerializer: PassportSerializerService);
    $onInit(): Promise<any>;
    $beforeRoutesInit(): void | Promise<any>;
}
