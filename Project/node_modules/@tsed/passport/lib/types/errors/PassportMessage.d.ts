import { Unauthorized } from "@tsed/exceptions";
export declare class PassportMessage extends Unauthorized {
    opts: Record<string, unknown>;
    constructor(message: string, opts?: Record<string, unknown>);
}
