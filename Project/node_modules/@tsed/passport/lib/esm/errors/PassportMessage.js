import { Unauthorized } from "@tsed/exceptions";
export class PassportMessage extends Unauthorized {
    opts;
    constructor(message, opts = {}) {
        super(message);
        this.opts = opts;
    }
}
//# sourceMappingURL=PassportMessage.js.map