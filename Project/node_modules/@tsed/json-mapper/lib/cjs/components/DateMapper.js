"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DateMapper = void 0;
const tslib_1 = require("tslib");
const core_1 = require("@tsed/core");
const jsonMapper_1 = require("../decorators/jsonMapper");
/**
 * Mapper for `Date` type.
 * @jsonmapper
 * @component
 */
let DateMapper = class DateMapper {
    deserialize(data) {
        // don't convert unexpected data. In normal case, Ajv reject unexpected data.
        // But by default, we have to skip data deserialization and let user to apply
        // the right mapping
        if ((0, core_1.isBoolean)(data) || data === null || data === undefined) {
            return data;
        }
        return new Date(data);
    }
    serialize(object) {
        return object ? new Date(object).toISOString() : object;
    }
};
DateMapper = tslib_1.__decorate([
    (0, jsonMapper_1.JsonMapper)(Date)
], DateMapper);
exports.DateMapper = DateMapper;
//# sourceMappingURL=DateMapper.js.map