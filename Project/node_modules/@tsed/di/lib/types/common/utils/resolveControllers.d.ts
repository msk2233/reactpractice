import { TokenProvider } from "../interfaces/TokenProvider";
import { TokenRoute } from "../interfaces/TokenRoute";
export declare function getTokens(config: any): {
    route?: string;
    token: TokenProvider;
}[];
/**
 * Return controllers and is base route according to his configuration in module configuration.
 *
 * @param settings
 */
export declare function resolveControllers(settings: Partial<TsED.Configuration>): TokenRoute[];
