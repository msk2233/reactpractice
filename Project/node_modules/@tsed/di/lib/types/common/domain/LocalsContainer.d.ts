import { Hooks } from "@tsed/core";
import type { TokenProvider } from "../interfaces/TokenProvider";
export declare class LocalsContainer extends Map<TokenProvider, any> {
    readonly hooks: Hooks;
    destroy(): Promise<void>;
}
