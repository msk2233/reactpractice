import { registerProvider } from "@tsed/di";
import { registerResponseFilter } from "../domain/ResponseFiltersContainer.js";
/**
 * Register a response filter service.
 * @param contentTypes
 * @decorator
 */
export function ResponseFilter(...contentTypes) {
    return (target) => {
        contentTypes.forEach((contentType) => {
            registerResponseFilter(contentType, target);
        });
        registerProvider({
            provide: target,
            useClass: target
        });
    };
}
//# sourceMappingURL=responseFilter.js.map