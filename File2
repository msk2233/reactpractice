import { createSlice, createAsyncThunk, PayloadAction } from "@reduxjs/toolkit";
import axiosInstance from "../api/axiosInstance";
import { forAuthslice } from "../interfaces/regInterface";

type User = {
  email: string;
  password: string;
};

type UserBasicInfo = {
  data: forAuthslice;
};

type AuthApiState = {
  basicUserInfo?: UserBasicInfo | null;
};

export const login = createAsyncThunk("login", async (data: User) => {
  const response = await axiosInstance.post("/login", data);
  const resData = response.data;
  localStorage.setItem("userInfo", JSON.stringify(resData.res));
  return resData.res;
});

export const register = createAsyncThunk(
  "register",
  async (data: forAuthslice) => {
    const response = await axiosInstance.post("/register", data);
    const resData = response.data;
    localStorage.setItem("userInfo", JSON.stringify(resData));
    return resData;
  }
);

export const logout = createAsyncThunk("logout", async () => {
  const response = await axiosInstance.post("/logout", {});
  const resData = response.data;
  localStorage.removeItem("userInfo");
  return resData;
});
export const getUser = createAsyncThunk(
  "users/profile",
  async (userId: string) => {
    const response = await axiosInstance.get(`/users/${userId}`);
    return response.data;
  }
);
const initialState: AuthApiState = {
  basicUserInfo: localStorage.getItem("userInfo")
    ? JSON.parse(localStorage.getItem("userInfo") as string)
    : null,
};
const authSlice = createSlice({
  name: "auth",
  initialState,
  reducers: {
    loginUser:(state,action:PayloadAction<UserBasicInfo>) =>{
        
    }
  },
  extraReducers: (builder) => {
    builder
      .addCase(
        login.fulfilled,
        (state, action: PayloadAction<UserBasicInfo>) => {
          state.basicUserInfo = action.payload;
        }
      );
    // .addCase(
    //   register.fulfilled,
    //   (state, action: PayloadAction<UserBasicInfo>) => {
    //     state.status = "idle";
    //     state.basicUserInfo = action.payload;
    //   }
    // )

    // .addCase(logout.fulfilled, (state, action) => {
    //   state.status = "idle";
    //   state.basicUserInfo = null;
    // })
    // .addCase(getUser.fulfilled, (state, action) => {
    //   state.status = "idle";
    //   state.userProfileData = action.payload;

  },
});

export default authSlice.reducer;
PUT ALL THE FUNCTIONALITY OF LOGIN,REGISTER,LOGOUT INSIDE SIMPLE REDUCER AND TELL ME HOW TO USE IT  
