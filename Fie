import { DataSource } from 'typeorm';
import { config } from 'dotenv';
config();

export default new DataSource({
  type: 'postgres',
  host: process.env.DATABASE_HOST,
  port: +process.env.DATABASE_PORT,
  username: process.env.DATABASE_USER,
  password: process.env.DATABASE_PASSWORD,
  database: process.env.DATABASE_DB,
  dropSchema: false,
  logging: false,
  entities: ["src/entity/*{.ts,.js}"],
  migrations: ['src/migrations/**/*.ts'],
  // seed:[__dirname + 'src/seeder/*.ts'],,'src/seeder/seed.ts'
  // migrationsTableName: 'migration_table',
  synchronize: true,
});

THIS IS MIGRATION

import { MigrationInterface, QueryRunner } from "typeorm";

export class Userdata1720076673600 implements MigrationInterface {
    name = 'Userdata1720076673600'

    public async up(queryRunner: QueryRunner): Promise<void> {
        await queryRunner.query(`CREATE TABLE "user" ("id" SERIAL NOT NULL, "fname" character varying NOT NULL, "lname" character varying NOT NULL, "email" character varying NOT NULL, "phno" character varying NOT NULL, CONSTRAINT "PK_cace4a159ff9f2512dd42373760" PRIMARY KEY ("id"))`);
    }

    public async down(queryRunner: QueryRunner): Promise<void> {
        await queryRunner.query(`DROP TABLE "user"`);
    }

}

this is package.json 



{
  "name": "typeorm-sample",
  "version": "0.0.1",
  "description": "Awesome project developed with TypeORM.",
  "devDependencies": {
    "ts-node": "10.9.1",
    "@types/node": "^16.11.10",
    "typescript": "4.5.2"
  },
  "dependencies": {
    "typeorm": "0.3.20",
    "reflect-metadata": "^0.1.13",
    "pg": "^8.4.0"
  },
  "scripts": {
    "start": "ts-node src/index.ts",
    "typeorm": "typeorm-ts-node-commonjs",
   "migration:run": "npm run typeorm migration:run -- -d ./src/data-source.ts",
    "migration:generate": "npm run typeorm -- -d ./src/data-source.ts migration:generate ./src/migration/$npm_config_name",
    "migration:create": "npm run typeorm -- migration:create ./src/migration/$npm_config_name",
    "migration:revert": "npm run typeorm -- -d ./src/data-source.ts migration:revert"
  }
}

